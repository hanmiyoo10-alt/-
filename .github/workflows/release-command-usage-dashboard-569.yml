name: Release Command Usage Dashboard 3.0.0-alpha.5.69

# Stage C transition trigger. This version-specific command is temporary and
# will be retired by Stage D after the generic controller is proven.
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/release-command-usage-dashboard-569.yml'

permissions:
  contents: write

jobs:
  promote:
    uses: ./.github/workflows/reusable-usage-dashboard-promote.yml
    with:
      candidate_sha: ${{ github.sha }}
      release_branch: release-usage-dashboard
