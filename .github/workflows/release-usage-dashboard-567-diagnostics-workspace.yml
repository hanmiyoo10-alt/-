name: Stage Usage Dashboard 3.0.0-alpha.5.67 Diagnostics Workspace Overhaul (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archive
        run: echo 'Archived release publisher. Historical validation remains in git history; this workflow cannot write main or the release channel.'
