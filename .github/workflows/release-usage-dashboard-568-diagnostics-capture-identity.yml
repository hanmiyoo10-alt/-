name: Stage Usage Dashboard 3.0.0-alpha.5.68 Diagnostics Capture Identity (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archive
        run: echo 'Archived release validator. Historical validation remains in git history; this workflow cannot write main or the release channel.'
