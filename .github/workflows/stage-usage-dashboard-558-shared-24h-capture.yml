name: Archived Usage Dashboard 3.0.0-alpha.5.58 Shared 24h Capture Coalescing

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Archived publisher. Historical validation remains in git history; this workflow no longer writes main or release-usage-dashboard.'
