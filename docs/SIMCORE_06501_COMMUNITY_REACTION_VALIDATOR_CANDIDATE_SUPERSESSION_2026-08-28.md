# SimCore v0.65.1 COMMUNITY Reaction Validator Candidate — Supersession Record

Date: 2026-08-28

Status: **SUPERSEDED / HISTORICAL CANDIDATE · DO NOT ACTIVATE FROM OLD PLAN · NO RUNTIME CHANGE**

Superseded historical plan:

`docs/SIMCORE_06501_COMMUNITY_REACTION_VALIDATOR_PLAN.md`

## 1. Purpose

The old v0.65.1 plan was a valid provisional design when it was written on 2026-08-23. It targeted repeated v0.64.3 COMMUNITY reaction-tail warnings and intentionally required exact static reproduction before any tolerance change.

Later source/evidence history changed the disposition before M2-3 finished.

This document prevents the old provisional version number and candidate status from being mistaken for the current next-release authority.

---

## 2. What happened after the old candidate was written

The warning family was narrowed through v0.64.4 attribution and then repaired in:

```text
v0.64.5 — COMMUNITY Multiline Reaction Unit Validation Repair
```

Authoritative live close:

`docs/SIMCORE_LIVE_06405_VALIDATION.md`

The direct real-long-chat result included multiple bilingual/multiline logical comment units where the reaction tag appears on the continuation/translation line.

Those natural controls produced:

```text
Warnings: 0
Compatibility diagnostics: 0
Output representation: EXACT
Deferred mirror: COMMITTED
```

and the high-value B_END control produced:

```text
Broadcast closure: COMPLETE · terminal EXPLICIT · structure PASS
Broadcast terminal coverage: EXPLICIT_TERMINAL
Stored broadcast: UNLOCKED
```

The v0.64.5 evidence classifies the motivating multiline family as:

```text
COMMUNITY_MULTILINE_REACTION_UNIT_MISMATCH
= RESOLVED / DIRECT_LIVE_CONTROL
```

---

## 3. Deferred-sweep authority

`docs/SIMCORE_DEFERRED_SWEEP_AFTER_06406.md` explicitly reclassifies the old Structure/Reaction watch:

```text
historical watch text remains evidence
specific multiline-warning family is superseded by v0.64.5 live PASS
no remaining WATCH item at that sweep satisfies promotion rules for an independent correctness fix
```

Therefore the old v0.65.1 plan must not be activated merely because its file still says `DESIGN FROZEN CANDIDATE`.

---

## 4. Current disposition

```text
old v0.65.1 version reservation
= CANCELLED / SUPERSEDED

old candidate implementation gate
= NO LONGER ACTIVE

motivating multiline validator family
= RESOLVED BY v0.64.5

next planned structural release after v0.65.0
= v0.66.0 / M2-4
```

Current v0.66.0 authority:

`docs/SIMCORE_06600_M2_4_SESSION_RUNTIME_MIRROR_BOUNDARY_COMPLETION_ACTIVATION.md`

---

## 5. What this supersession does NOT claim

This does not claim that every possible future COMMUNITY/Reaction warning is impossible.

A later natural warning may represent:

```text
a genuinely malformed generated comment
a different platform-family shape
a stale-scale normalization observation
a new validator defect
a recurrence of an old family under a different cause
```

Any future specimen must be reviewed from its own RAW input/output and full diagnostic packet before classification.

Do not resurrect the old v0.65.1 implementation automatically.

Correct future process:

```text
new natural specimen
→ full per-packet diagnostic review
→ compare against v0.64.5 resolved family
→ establish whether it is SAME FAMILY / DISTINCT / UNRESOLVED
→ promote only from current evidence
```

---

## 6. Historical preservation rule

Do not delete or rewrite the original v0.65.1 plan merely because it is superseded.

It remains useful historical design evidence showing:

```text
what was known at the time
what implementation restraint was required
why broad grammar tolerance was rejected
how the later v0.64.5 repair differed from the earlier hypothesis
```

This supersession record is authoritative for **current release planning**, while the old plan remains authoritative only as historical context.

---

## 7. Final verdict

```text
SIMCORE_06501_COMMUNITY_REACTION_VALIDATOR_PLAN
= HISTORICAL
= SUPERSEDED
= NOT NEXT RELEASE AUTHORITY

REACTION MULTILINE FAMILY
= RESOLVED BY v0.64.5 LIVE EVIDENCE

NEXT DESIGN AUTHORITY
= v0.66.0 M2-4 Session / Runtime Mirror Boundary Completion
```
