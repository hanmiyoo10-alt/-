# SimCore CACHE-A3 — Canonical Equivalence Shadow Descriptor

Date: 2026-09-03 KST
Status: **CLOSED · SHADOW DESCRIPTOR LOCKED · 18/18 EXACT-BYTE EQUIVALENCE · SERIALIZER CONSOLIDATION NOT JUSTIFIED · PRODUCTION RUNTIME UNCHANGED**
Classification: **SIMCORE CACHE PROGRAM · CACHE-A3 · CANONICAL EQUIVALENCE · PROMPT SEGMENT DESCRIPTOR · NO-OP SERIALIZER DECISION**

## 1. Decision

CACHE-A3 is closed as a shadow-only canonical-equivalence checkpoint on top of the permanent CACHE-A2 exact-byte oracle.

A3 does not add a new production serializer.

A2 already proved that the current production compiler has no demonstrated canonicalization defect in the tested seams:

```text
active T1 stable bytes
= exact across the representative matrix

reaction_max reordered source-object construction
= exact byte identity

reload-equivalent semantic state
= exact byte identity

T6 operational noise
= zero prompt-byte authority
```

The master cache architecture says serializer consolidation is conditional:

```text
ONLY IF NEEDED
→ consolidate stable serialization behind one owner
→ reproduce already-qualified bytes first
```

No such need is proven by A2/A3.

Therefore the correct A3 outcome is:

```text
CANONICAL EQUIVALENCE LOCK
= IMPLEMENTED AS SHADOW VALIDATION

PRODUCTION SERIALIZER CONSOLIDATION
= NOT_JUSTIFIED_NO_OP
```

This preserves repository change doctrine:

```text
WORKING + VERIFIED
→ PRESERVE
```

---

## 2. Exact authority baseline

A3 began from exact main:

```text
main
= 1da4b507a02b1d9ca30014d99ee57eba697f5125
```

Production runtime authority remained:

```text
release-simcore
= 861100f4771967aa5b8ab8811d06f11702c0d3ff

production SimCore
= v0.70.1 Cold First-Turn Tail Attribution

PROMPT_COMPILER_VERSION
= 4
```

A3's machine oracle is the permanent A2 golden:

```text
fixtures/simcore/cache-a2-prompt-fixtures-v0701.json

schema
= SimCoreCacheA2ExactByteFixtureBaselineV1
```

Production prompt bytes are not regenerated by an A3-specific semantic compiler.

A3 reads A2's already-qualified exact prompt/tier bytes and derives metadata around them.

---

## 3. Permanent A3 artifacts

A3 adds:

```text
scripts/simcore-cache-a3-shadow-descriptors.mjs
fixtures/simcore/cache-a3-shadow-descriptors-v0701.json
docs/SIMCORE_CACHE_A3_CANONICAL_EQUIVALENCE_SHADOW_DESCRIPTOR_2026-09-03.md
```

The machine-readable output schema is:

```text
SimCoreCacheA3ShadowDescriptorBaselineV1
```

The descriptor vocabulary is a bounded shadow implementation of the master architecture's conceptual:

```text
PromptSegmentDescriptorV1
```

A3 names its concrete non-runtime form:

```text
PromptSegmentDescriptorV1_SHADOW
```

This distinction matters.

```text
SHADOW DESCRIPTOR
!=
PRODUCTION PROMPT IR
!=
SEMANTIC AUTHORITY
!=
PROVIDER CACHE PLAN AUTHORITY
```

---

## 4. Descriptor contract

Each A3 segment records the master-design fields where evidence exists:

```text
id
owner
semanticRole
stabilityClass
orderClass
modelVisibility
canonicalizationPolicy
semanticFingerprint
byteFingerprint
breakAuthority
providerHintEligibility
providerHintPolicy
sourceReference
```

A3 additionally records exact physical evidence:

```text
serializedText
byteLength
physicalOrder
separatorBefore
physicalByteRange
physicalLineRange
```

The reconstruction contract is:

```text
for segments in physicalOrder:
  concatenate separatorBefore + serializedText
```

Required result:

```text
reconstructed bytes
= exact A2 fixture bytes

reconstructed SHA-256
= exact A2 fixture SHA-256

reconstructed UTF-8 byte length
= exact A2 fixture byte length
```

Any mismatch fails the A3 validator.

---

## 5. Exact equivalence result

A3 evaluated all concrete A2 fixture records.

Observed:

```text
fixtureCount
= 18

activeFixtureCount
= 17

exactByteEquivalentCount
= 18

failed reconstruction
= 0
```

Therefore:

```text
A2 GOLDEN PROMPT
→ SHADOW SEGMENT DESCRIPTORS
→ EXACT PHYSICAL RECONSTRUCTION
→ SAME BYTES
```

for every current A2 fixture.

This is the first machine-enforced prompt-segment representation that proves it can describe the current prompt without altering it.

---

## 6. T1 remains exact

Every active fixture still exposes one exact primary stable-contract descriptor:

```text
SIMCORE_T1_STABLE_CONTRACT
```

Observed:

```text
uniqueActiveT1FingerprintCount
= 1

activeT1ByteFingerprint
= eb120b401add80522a802971d905ce00856849b7bbb353d00f361155678b6537
```

This exactly matches the closed A2 stable-tier fingerprint.

Therefore A3 does not create a new T1 identity.

It carries forward the A2 identity.

```text
A3 DESCRIPTOR METADATA CHANGE
!=
T1 CACHE ABI CHANGE
```

---

## 7. Current physical segmentation

A3 describes the current SimCore prompt in physical order.

For an ordinary active fixture the shadow sequence is approximately:

```text
SIMCORE_T1_STABLE_CONTRACT
↓
SIMCORE_T3_KOREAN_AGE_OFFSET
SIMCORE_T3_WORLD_YEAR
SIMCORE_T3_SECONDARY_CONFIGURATION
SIMCORE_T5_SECONDARY_ACTIVE
SIMCORE_T3_EPISODE_NO
↓
SIMCORE_T5_MODE_STATE
↓
optional SIMCORE_T5_CONDITIONAL_GUIDANCE
optional SIMCORE_T5_HOT_REACTION_STATE
↓
SIMCORE_MIXED_FINAL_REQUIRED_BLOCKS
↓
SIMCORE_T1_CLOSING_FRAME_PHYSICAL_TAIL
```

This is a classification of the bytes that exist today.

It is not a target layout.

---

## 8. `slowLines` physical region is now mechanically decomposed

A2 proved the compiler-native `slow` tier is architecturally mixed.

A3 turns that proof into strict machine classification.

Allowed current slow-line descriptors are:

```text
korean_age_offset=
→ SIMCORE_T3_KOREAN_AGE_OFFSET

current_korean_age=
→ SIMCORE_T3_CURRENT_KOREAN_AGE_DERIVED

world_year=
→ SIMCORE_T3_WORLD_YEAR

secondary_configured=
→ SIMCORE_T3_SECONDARY_CONFIGURATION

secondary_active=
→ SIMCORE_T5_SECONDARY_ACTIVE

episode_no=
→ SIMCORE_T3_EPISODE_NO
```

Any unknown current slow line fails A3 rather than silently receiving a guessed class.

This creates a useful future change gate:

```text
NEW SLOW-LINE SHAPE
→ CANNOT SILENTLY INHERIT T3/T5 CLASSIFICATION
→ OWNER / LIFETIME MUST BE EXPLICITLY ADDED
```

---

## 9. Volatile physical region

A3 uses strict existing anchors for the compiler-native volatile tier.

The first three lines must remain:

```text
mode=
broadcast_locked=
community_blocks_expected=
```

They form:

```text
SIMCORE_T5_MODE_STATE
```

Middle conditionally emitted lines become:

```text
SIMCORE_T5_CONDITIONAL_GUIDANCE
```

when present.

`reaction_max=` becomes:

```text
SIMCORE_T5_HOT_REACTION_STATE
```

when present.

A3 does not copy the production conditional templates into a second semantic implementation.

It only groups their already-qualified A2 bytes between known physical anchors.

---

## 10. Mixed footer line remains mixed

The current line:

```text
final_required_blocks=COMMUNITY:<current>,Knowledge:1_last
```

contains:

- stable footer framing,
- current dynamic Community-count value.

A3 classifies the physical line as:

```text
SIMCORE_MIXED_FINAL_REQUIRED_BLOCKS
stabilityClass = MIXED_T1_T5
```

and freezes:

```text
canonicalizationPolicy
= PRESERVE_MIXED_LINE_EXACTLY_UNTIL_SEPARATE_OWNED_REFACTOR
```

A3 deliberately does not split this line into synthetic production bytes.

```text
LOGICAL SUBFIELD DIFFERENCE
!=
AUTHORIZATION TO REWRITE A WORKING PHYSICAL LINE
```

---

## 11. Stable closing frame is physically after dynamic state

The final production line is:

```text
[/SIMCORE CORE STATE]
```

It is stable framing, so A3 classifies it as T1 semantics:

```text
SIMCORE_T1_CLOSING_FRAME_PHYSICAL_TAIL
```

but records:

```text
orderClass
= PHYSICAL_TAIL_DO_NOT_MOVE_IN_A3
```

and provider posture:

```text
SHADOW_ONLY_PHYSICALLY_AFTER_T5
```

This is another example of the architecture's critical distinction:

```text
LOGICAL STABILITY CLASS
!=
CURRENT PHYSICAL PREFIX POSITION
```

A3 does not move the closing frame.

---

## 12. Segment count result

Across all 17 active fixtures the generated descriptor corpus contains:

```text
T1
= 34 descriptor instances

T3
= 69 descriptor instances

T5
= 58 descriptor instances

MIXED_T1_T5
= 17 descriptor instances
```

The 34 T1 instances are expected:

```text
17 × SIMCORE_T1_STABLE_CONTRACT
+
17 × SIMCORE_T1_CLOSING_FRAME_PHYSICAL_TAIL
```

These are descriptor instances across fixtures, not 34 distinct T1 contracts.

The stable-core byte identity remains one fingerprint.

---

## 13. Semantic fingerprint firewall

The conceptual master descriptor includes:

```text
semanticFingerprint
```

A3 intentionally does **not** derive it.

Every A3 shadow descriptor records:

```text
semanticFingerprint
= null

semanticFingerprintStatus
= NOT_DERIVED_IN_A3_SHADOW
```

Reason:

```text
SOURCE BYTES EXIST
!=
SEMANTIC IDENTITY PROVEN
```

and:

```text
SAME BYTES
!=
A GENERAL SEMANTIC EQUIVALENCE PROOF
```

A semantic fingerprint must eventually be derived from an authorized semantic-input contract, not invented by hashing rendered text and relabeling that hash as semantics.

This preserves the same grounding boundary used elsewhere in the repository:

```text
MECHANICAL EVIDENCE
!=
SEMANTIC AUTHORITY
```

---

## 14. Byte fingerprint authority

A3 does own exact byte fingerprints.

Every descriptor records:

```text
byteFingerprint
= SHA-256(serializedText)
```

The fixture records both:

```text
a2PromptSha256
reconstructedPromptSha256
```

and requires equality.

Therefore A3 can mechanically answer:

```text
Did this descriptor representation preserve the exact golden bytes?
```

A3 cannot answer by itself:

```text
Is a proposed different byte sequence semantically equivalent?
```

A different-byte equivalence claim would require separate semantic evidence and explicit authorization.

---

## 15. Provider-hint firewall

The master descriptor vocabulary anticipates future provider hint eligibility.

A3 keeps this non-authoritative.

Default:

```text
providerHintEligibility
= UNDECIDED

providerHintPolicy
= NO_PROVIDER_TRANSPORT_DECISION_IN_A3
```

The stable core is only marked:

```text
ARCHITECTURAL_CANDIDATE_ONLY
```

The closing T1 frame records only its physical shadow condition.

A3 does not emit:

- cache-control directives,
- explicit cache keys,
- provider TTLs,
- provider cache breakpoints,
- provider read/write claims,
- provider hit/miss claims.

Provider cache remains:

```text
UNVERIFIED
```

---

## 16. Serializer consolidation decision

The master architecture allowed A3 to consolidate stable serialization **only if needed**.

A2/A3 evidence instead says:

```text
T1 current bytes deterministic across active matrix
reaction_max existing deterministic serialization works
reload-equivalent state works
T6 firewall works
shadow descriptor reconstruction works 18/18
```

No proven serializer defect exists.

Therefore:

```text
serializerConsolidation
= NOT_JUSTIFIED_NO_OP
```

This is not a permanent claim that the existing compiler can never change.

It is the correct current decision:

```text
NO PROVEN GAP
→ NO REWRITE
```

If a future fixture exposes nondeterministic or semantically duplicated serialization, repair that narrow seam under its own evidence.

---

## 17. Canonicalization policy

A3's default policy is intentionally conservative:

```text
PRESERVE_A2_GOLDEN_BYTES
```

This means the first compatibility mode of any future canonical serializer is now machine-testable:

```text
current A2 semantic fixture
→ proposed representation/serializer
→ exact A2 bytes
```

A future serializer cannot claim compatibility merely because the output looks equivalent.

The default compatibility gate is byte equality.

Only an explicitly authorized semantic incompatibility may establish a new golden baseline.

---

## 18. What A3 does not prove

A3 does not prove:

- provider prompt-cache reads,
- provider prompt-cache writes,
- provider cache hit rate,
- provider cache TTL behavior,
- monetary cache savings,
- request-prefix placement optimality,
- host-prefix stability,
- history-prefix stability beyond existing evidence,
- semantic equivalence of different byte sequences,
- that T1 should move before chat history/current user,
- that the mixed footer should be split,
- that a new serializer should be introduced,
- that a Prompt Cache ABI revision should advance,
- that production release identity should change.

---

## 19. Runtime/release impact

A3 changes no production runtime file.

Frozen non-effects:

```text
plugins/simcore/latest.js
= unchanged

plugins/simcore/install.js
= unchanged

release-simcore
= unchanged

PROMPT_COMPILER_VERSION
= 4 unchanged

runtimePromptPlacement
= TAIL_AFTER_CURRENT_USER unchanged

runtimePromptPolicy
= OBSERVE_ONLY unchanged

persistent schema
= unchanged

provider cache controls
= none added
```

A3 is repository tooling/evidence only.

---

## 20. Closure checklist

```text
A2 golden selected as sole byte oracle
= PASS

18 fixture descriptors generated
= PASS

18/18 exact prompt reconstruction
= PASS

18/18 SHA equality
= PASS

active T1 stable fingerprint remains unique
= PASS / 1

slowLines unknown fallback
= NONE / FAILS CLOSED

volatile mode anchors asserted
= PASS

footer anchors asserted
= PASS

mixed footer preserved without rewrite
= PASS

physical tail closing frame preserved
= PASS

semanticFingerprint self-declaration
= 0

provider transport decision
= 0

serializer consolidation
= NOT_JUSTIFIED_NO_OP

production runtime mutation
= 0

release-simcore mutation
= 0
```

Result:

```text
CACHE-A3
= CLOSED / CANONICAL EQUIVALENCE SHADOW DESCRIPTOR LOCKED
```

---

## 21. Next checkpoint

The master cache architecture's next named checkpoint is:

```text
CACHE-A4
= OBSERVER COLD-COST EXACT ATTRIBUTION
```

That checkpoint is distinct from A3.

A4 may resume the existing cold-path attribution design only under a fresh execution preflight and a runtime release identity selected above current production at execution time.

A3 does not authorize that release by itself.

Before A4 optimization decisions, preserve the architecture rule:

```text
FIRST MEASURE
→ THEN ATTRIBUTE
→ THEN OPTIMIZE
```

---

## 22. Final frozen invariant

```text
THE A2 GOLDEN IS STILL THE BYTE ORACLE.

A3 CAN DESCRIBE IT
WITHOUT CHANGING IT.

18 / 18 FIXTURES
RECONSTRUCT EXACTLY.

T1 REMAINS ONE EXACT STABLE CORE FINGERPRINT.

slowLines IS NOW STRICTLY MACHINE-CLASSIFIED
INTO ITS PROVEN T3 / T5 SUBFIELDS.

MIXED PHYSICAL BYTES STAY MIXED
UNTIL A SEPARATE OWNED REFACTOR IS JUSTIFIED.

BYTES CAN PROVE BYTE IDENTITY.
BYTES DO NOT SELF-PROVE SEMANTIC IDENTITY.

SHADOW METADATA DOES NOT AUTHORIZE PROVIDER CACHE TRANSPORT.

NO PROVEN SERIALIZER GAP
→ NO SERIALIZER REWRITE.

PRODUCTION STAYS UNCHANGED.
```
