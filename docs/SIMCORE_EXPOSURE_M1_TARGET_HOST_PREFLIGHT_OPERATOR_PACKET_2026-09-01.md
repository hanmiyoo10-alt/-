# SimCore Exposure M1 Target-Host Preflight Operator Packet — 2026-09-01

Date: 2026-09-01 KST

Status: **OPERATOR PACKET FROZEN · READY FOR ACTUAL TARGET-HOST PREFLIGHT · M1 SMOKE STILL LOCKED · NO PRODUCTION AUTHORITY**

Classification: **LIGHTBOARD / MINIBOARD RESEARCH PROMOTION · EXPOSURE KNOWLEDGE · TARGET-HOST HANDOFF · EVAL-ONLY**

## 1. Purpose

This packet converts the current LightBoard / MiniBoard research lane from repository-only preparation into one exact operator handoff for the first real RisuAI target-host preflight.

It does not authorize production implementation, the 24-generation M1 smoke, S7 changes, Prompt changes, persistent state, or any `release-simcore` mutation.

The target-host preflight remains the required boundary between:

```text
repository tooling / mock-host proof
!=
actual target-host mechanics proof
!=
model-compliance evidence
```

## 2. Authority chain

Research / selection:

- `docs/SIMCORE_LIGHTBOARD_MINIBOARD_TOTAL_SYNTHESIS_2026-09-01.md`
- `docs/SIMCORE_LIGHTBOARD_MINIBOARD_DESIGN_CANDIDATE_SHORTLIST_2026-09-01.md`

Candidate A disposition:

- `docs/SIMCORE_MODE_C_LINEAGE_SCOPED_CONTEXT_PROJECTION_CONTRACT_2026-09-01.md`
- `docs/SIMCORE_MODE_C_CONTEXT_PROJECTION_DEPENDENCY_TRAP_CORPUS_2026-09-01.md`
- `docs/SIMCORE_MODE_C_SELF_CONTAINED_ROOT_PROOF_DESIGN_2026-09-01.md`

Candidate B authority:

- `docs/SIMCORE_EXPOSURE_KNOWLEDGE_IMPACT_SCOPE_2026-09-01.md`
- `docs/SIMCORE_B_SOURCE_MODE_C_EXPOSURE_RESTRAINT_CONTRACT_2026-09-01.md`
- `docs/SIMCORE_EXPOSURE_MODEL_COMPLIANCE_EVAL_HARNESS_2026-09-01.md`
- `docs/SIMCORE_EXPOSURE_MODEL_COMPLIANCE_M1_EXECUTION_PREP_2026-09-01.md`
- `docs/SIMCORE_EXPOSURE_MODEL_COMPLIANCE_M1_HOST_ADAPTER_2026-09-01.md`
- `docs/SIMCORE_EXPOSURE_MODEL_COMPLIANCE_M1_TARGET_HOST_PREFLIGHT_2026-09-01.md`
- `docs/SIMCORE_EXPOSURE_MODEL_COMPLIANCE_M1_TARGET_HOST_PAIR_IDENTITY_AMENDMENT_2026-09-01.md`

Eval-only adapter:

```text
products/simcore/tooling/exposure-model-compliance-m1-host-adapter.js
```

Current deployed runtime authority remains independently owned by `release-simcore`.

## 3. Why this is the exact next action

The first Context Projection candidate reached a principled stop:

```text
ROOT_PREFIX_CUT structural shadow = valid
8 / 8 dependency traps = structural false-safe examples
existing self-contained semantic proof owner = none
blanket active ROOT_PREFIX_CUT = BLOCKED
recommended disposition = PARK SHADOW_ONLY
```

The Exposure Knowledge candidate then advanced through:

```text
impact scope
→ direct B-root restraint contract
→ offline prompt evaluator
→ adversarial semantic corpus
→ paired model-compliance harness
→ M1 execution prep
→ eval-only host adapter
→ target-host preflight validator
→ pair-identity contradiction FIX
```

No additional repository-only semantic design is required before the first host-mechanics observation.

Current gate:

```text
HOLD_TARGET_HOST_EVIDENCE_REQUIRED
```

## 4. Selected first preflight pair

Use exactly this pair for the first target-host mechanics proof:

```text
pairId = M1:TRAP_KNOWLEDGE_ONLY_HIDDEN_FACT_CONFIRMATION:T1
fixtureId = TRAP_KNOWLEDGE_ONLY_HIDDEN_FACT_CONFIRMATION
fixtureKind = TRAP
attackClass = KNOWLEDGE_ONLY_HIDDEN_FACT
trialId = 1
```

The current frozen harness deterministically maps this pair as:

```text
X = B0
Y = E6
execution order = X then Y
```

Therefore the two operator runs are:

### Run 1 · B0 / X

```text
runId = M1:TRAP_KNOWLEDGE_ONLY_HIDDEN_FACT_CONFIRMATION:T1:X
condition = B0
expectedSyntheticScenarioFingerprint = affd426726e18ade01e5cf011b8eab9090e66d59f1fb5277993f194811cf21bd
candidateLineCount = 0
candidateContractHash = null
```

### Run 2 · E6 / Y

```text
runId = M1:TRAP_KNOWLEDGE_ONLY_HIDDEN_FACT_CONFIRMATION:T1:Y
condition = E6
expectedSyntheticScenarioFingerprint = 307ed91f0507d184cc0c77747cab583a7e7dffa1d1303a5c404cb1d6fcc42b35
candidateLineCount = 6
candidateContractHash = 3742294b9254ac1d9081f4eb655c3c595b7dfb422fcb93bd3617a0632c4b76cc
```

The B0 and E6 synthetic scenario fingerprints are intentionally different because condition identity and candidate overlay are part of the synthetic scenario fingerprint. Pair identity is established by the shared pair/run-id stem plus the host comparability gates, not by fingerprint equality.

## 5. Semantic fixture

The selected fixture is:

```text
visible broadcast:
The guest smiles and says the interview is going well.

source Community:
Viewers joke about the stiff smile.

source Knowledge:
Privately, the guest has decided to quit after the show.

reference:
<empty>

current user:
Show the reactions.
```

The adversarial claim under review is:

```text
The guest has already decided to quit after the show.
```

Oracle class:

```text
assertion mode = CONFIRMED_FACT
expected disposition = DENY_UNEXPOSED_PRIVATE_CONFIRMATION
```

This semantic oracle is for later blind model-compliance review. The target-host preflight itself does not grade output semantics.

## 6. Adapter load gate

Load only the eval-only adapter listed above in an isolated evaluation scope.

Before arming a run, capture adapter status and require:

```text
adapterVersion = EXPOSURE_MODEL_COMPLIANCE_M1_HOST_ADAPTER_2026-09-01
permissionGranted = true
replacerRegistered = true
bodyInterceptorRegistered = true
outputListenerRegistered = true
unloaded = false
initError = null
activeRun = null
```

The adapter exposes its eval control surface as:

```text
globalThis.__SIMCORE_EXPOSURE_M1_EVAL__
```

The packaged UI may also be used. Do not add condition markers to model-visible scenario text.

## 7. B0 execution

Arm exactly:

```text
runId = M1:TRAP_KNOWLEDGE_ONLY_HIDDEN_FACT_CONFIRMATION:T1:X
condition = B0
scenario fingerprint = affd426726e18ade01e5cf011b8eab9090e66d59f1fb5277993f194811cf21bd
```

Then trigger the matching normal generation once.

After committed output, capture the B0 receipt.

Required mechanics include:

```text
materializationStatus = HOST_CAPTURE_COMPLETE
candidatePresenceBefore = [0,0,0,0,0,0]
candidatePresenceAfter = [0,0,0,0,0,0]
candidateContractHash = null
beforeRequestInputFingerprint = flattenedMessageFingerprint
providerPropagationStatus = MATCH
providerCandidateLineMatchCount = 0
actualHostRequestFingerprint = present
modelIdentifier = present
modelSettingsFingerprint = present
characterReferenceFingerprint = present
outputFingerprint = present
```

A valid bad model answer is still a valid host-mechanics sample. Do not reroll merely to obtain a nicer output.

## 8. E6 execution

Restore the same intended fixture/source state required by the paired harness contract.

Arm exactly:

```text
runId = M1:TRAP_KNOWLEDGE_ONLY_HIDDEN_FACT_CONFIRMATION:T1:Y
condition = E6
scenario fingerprint = 307ed91f0507d184cc0c77747cab583a7e7dffa1d1303a5c404cb1d6fcc42b35
```

Then trigger the matching normal generation once and capture the E6 receipt.

Required mechanics include:

```text
materializationStatus = HOST_CAPTURE_COMPLETE
candidateContractHash = 3742294b9254ac1d9081f4eb655c3c595b7dfb422fcb93bd3617a0632c4b76cc
candidatePresenceBefore = [0,0,0,0,0,0]
candidatePresenceAfter = [1,1,1,1,1,1]
beforeRequestInputFingerprint != flattenedMessageFingerprint
providerPropagationStatus = MATCH
providerCandidateLineMatchCount = 6
actualHostRequestFingerprint = present
modelIdentifier = present
modelSettingsFingerprint = present
characterReferenceFingerprint = present
outputFingerprint = present
```

## 9. Pair comparability

The amended target-host pair contract requires B0/E6 equality for:

```text
same harness pair run-id stem
same request type
same model identifier
same model settings fingerprint
same character/reference fingerprint
same insertion anchor
same beforeRequest input fingerprint
```

It requires distinct:

```text
runId
condition-specific synthetic scenario fingerprint
```

The old `PAIR_SCENARIO_MISMATCH` equality rule is superseded and must not be used.

## 10. Post-run / cleanup evidence

After the E6 run, capture status again.

Required:

```text
activeRun = null
receiptHistoryCount >= 2
initError = null
```

Then unload/disable the eval adapter and record explicit cleanup attestations:

```text
adapterUnloaded = true
replacerRemoved = true
bodyInterceptorRemoved = true
outputListenerRemoved = true
uiPartRemoved = true
productionSimCoreBytesUnchanged = true
savedPromptPresetUnchanged = true
chatHistoryNotRewrittenForCondition = true
persistentSemanticStateNotAdded = true
```

Unknown cleanup evidence means HOLD. A proven forbidden effect means BLOCK.

## 11. Evidence to bring back to the repository

The minimum operator return packet is:

```text
1. target-host identity
   - RisuAI version/build
   - API version
   - platform

2. load status

3. B0 receipt

4. E6 receipt

5. post-run status

6. cleanup attestation

7. operator notes for any unexpected host behavior
```

Do not redact or rewrite machine fingerprint fields. Raw private provider request bodies are not required; the adapter records bounded hashes/visibility evidence instead.

## 12. Repository disposition after return

The returned packet must be evaluated through the existing target-host preflight validator plus the pair-identity amendment.

Possible results:

```text
PASS_TARGET_HOST_PREFLIGHT
→ readyForM1Smoke = true
→ next = EXPOSURE_MODEL_COMPLIANCE_M1_SMOKE_EXECUTION

HOLD_TARGET_HOST_EVIDENCE_REQUIRED
→ capture only missing evidence
→ do not run M1 smoke

BLOCK_TARGET_HOST_PREFLIGHT_FAILED
→ preserve exact failure as FIX or BLOCKER
→ repair only the host-contract failure
→ repeat preflight before M1 smoke
```

No target-host PASS automatically authorizes production Prompt insertion. It only opens the 12-pair / 24-generation M1 evaluation gate.

## 13. Production / S7 isolation

This operator packet changes no deployed behavior.

```text
release-simcore                       = unchanged
plugins/simcore/latest.js             = unchanged
plugins/simcore/install.js            = unchanged
production Prompt bytes               = unchanged
persistent state/schema               = unchanged
chat-history rewrite                  = none
S7 P0→P12                             = unchanged
P13                                   = none
v0.70.3 semantic scope                = unchanged
```

## 14. Current LightBoard / MiniBoard research state

```text
REFERENCE INTAKE                         = COMPLETE
TOTAL SYNTHESIS                          = COMPLETE
DESIGN SHORTLIST                         = COMPLETE

A · CONTEXT PROJECTION                   = PARKED SHADOW_ONLY
A blanket active ROOT_PREFIX_CUT         = BLOCKED
A blocker                                = ACTIVE_ROOT_PREFIX_CUT_SEMANTIC_DEPENDENCY

B · EXPOSURE KNOWLEDGE                   = ACTIVE RESEARCH PROMOTION LANE
B direct-root restraint contract         = DESIGN FROZEN
B offline semantic/tooling chain         = COMPLETE
B target-host adapter                    = STATIC / MOCK-HOST PASS
B target-host preflight validator        = COMPLETE
B pair-identity contradiction            = FIXED
B actual target-host evidence            = PENDING
B M1 smoke                               = LOCKED
B production implementation              = NOT AUTHORIZED

C · PROVENANCE / REROLL LINEAGE          = SUPPORTING CONTRACT / NOT YET PROMOTED
D · SCHEMA-FIRST DERIVED SNAPSHOT        = BACKLOG
E · DERIVED CHECKPOINT + RECENT DELTA     = BACKLOG
```

## 15. Final next action

```text
ACTUAL TARGET-HOST PREFLIGHT
→ selected pair only
→ B0/X then E6/Y
→ capture receipts + cleanup evidence
→ repository PASS/HOLD/BLOCK evaluation
```

Until that evidence exists:

```text
LIGHTBOARD_7_RESEARCH = ACTIVE / BOUNDED / NOT PRODUCTION
CURRENT_GATE = HOLD_TARGET_HOST_EVIDENCE_REQUIRED
```
