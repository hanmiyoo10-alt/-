# SimCore LightBoard Research Live Deferral and Next Research Decision — 2026-09-01

Date: 2026-09-01 KST

Status: **DECISION RECORDED · EXPOSURE TARGET-HOST PREFLIGHT DEFERRED TO v0.70.3 REAL-LONG-CHAT WINDOW · NEXT RESEARCH RECOMMENDATION = SOURCE INTELLIGENCE IMPACT SCOPE · NO IMPLEMENTATION AUTHORITY**

Classification: **LIGHTBOARD / MINIBOARD RESEARCH · EXPOSURE KNOWLEDGE · 3.0M ROADMAP · OPERATING DECISION**

## 1. Decision

The actual target-host B0/E6 preflight for the current Exposure Knowledge candidate is intentionally deferred.

Planned execution window:

```text
v0.70.3 Post-M2 Simplification Convergence real-long-chat validation
+ separately controlled Exposure target-host preflight evidence capture
```

This does not merge the two authorities. v0.70.3 remains the S7 simplification-convergence release lane; Exposure remains eval/research-only unless separately promoted after evidence.

## 2. Current Exposure disposition

Preserve the existing state:

```text
DIRECT_B_ROOT_EXPOSURE_CONTRACT = DESIGN FROZEN
TARGET_HOST_OPERATOR_PACKET = READY
TARGET_HOST_EVIDENCE = DEFERRED
M1 24-RUN SMOKE = LOCKED
PRODUCTION IMPLEMENTATION = NOT AUTHORIZED
```

The existing operator packet remains the exact execution authority when the real-host window opens:

```text
docs/SIMCORE_EXPOSURE_M1_TARGET_HOST_PREFLIGHT_OPERATOR_PACKET_2026-09-01.md
```

No new adapter, Prompt candidate, or production mutation is required before that window.

## 3. Why Candidate C is not the automatic next step

The LightBoard / MiniBoard shortlist classifies:

```text
C · Derived Provenance and Reroll Lineage
= TIER A-DEPENDENCY · DESIGN_PREP
= PROMOTE AFTER A/B DEFINE A CONCRETE DERIVED OBJECT
```

Current A/B results do not yet justify a generic C implementation/design program:

- Context Projection A is parked as SHADOW_ONLY after semantic-dependency counterexamples.
- Exposure B currently adds a bounded renderer restraint candidate, not a persisted derived source object requiring a new provenance identity system.

Therefore opening C as a standalone lineage platform would violate the shortlist's consumer-driven promotion rule.

## 4. Why D/E remain backlog

```text
D · Schema-First Derived Snapshot
= HOLD FOR CONCRETE CONSUMER

E · Derived Checkpoint + Recent Delta
= DEFER UNTIL PROJECTION EVIDENCE
```

Neither has a stronger current consumer than the Source Intelligence product hypothesis. Do not create a generic snapshot/checkpoint subsystem merely to keep the research lane moving.

## 5. Recommended next research transaction

Recommended next transaction:

```text
SOURCE_INTELLIGENCE_FIRST_IMPACT_SCOPE
```

Purpose:

Take the already completed LightBoard / MiniBoard synthesis and the A/B research results and scope the smallest credible SimCore-native Source Intelligence capability surface without freezing 3.0M or authorizing implementation.

Primary inputs:

```text
LightBoard / MiniBoard total synthesis
Context Projection negative/shadow result
Exposure Knowledge direct-B-root contract/eval tooling
3.0M Source Intelligence candidate map
current release-simcore architecture authority
```

Questions to answer:

```text
1. What exactly is a source sidecar in SimCore terms?
2. Which existing owner supplies source identity?
3. What source families share a minimum semantic contract?
4. Which fields are canonical vs derived vs presentation-only?
5. How are exposure, provenance and epistemic quarantine represented without a second world truth?
6. What happens on reroll/edit/source replacement?
7. What is the smallest first generalized source family?
8. Can the first design begin with zero auxiliary model calls and zero unbounded history?
9. Where would Candidate C become genuinely required by a concrete derived object?
10. What boundaries must remain isolated from S7 and production v0.70.x?
```

## 6. Why Source Intelligence is the preferred next research surface

The current 3.0M candidate map already ranks:

```text
1. Source Intelligence / Multi-Source Sidecar Runtime
2. Context Intelligence / Long-Chat Projection Engine
3. Semantic Sidecar Runtime / Auxiliary Job Platform
```

The Source Intelligence envelope directly absorbs the strongest LightBoard-derived concepts:

```text
Channel Reachability
→ Knowledge Boundary
→ Graded Exposure
→ Reaction / Publication Maturity
→ Source Coverage
→ Source Assertion
→ Assertion Provenance
→ Epistemic Quarantine
→ Bounded Context Projection
→ Validation
→ Renderer
```

This makes it the cleanest place to convert reference research into a product-shaped design hypothesis without forcing Candidate C/D/E in isolation.

## 7. Frozen boundaries

This decision changes no runtime/release authority:

```text
release-simcore = unchanged
production version = unchanged
latest.js/install.js = unchanged
S7 scope = unchanged
v0.70.3 semantic scope = unchanged
persistent schema = unchanged
Exposure E6 production install = none
3.0M scope freeze = none
3.0M implementation authority = none
```

## 8. Current sequence

```text
NOW
  record Exposure live deferral
  ↓
NEXT RESEARCH
  Source Intelligence first impact scope
  ↓
SEPARATE S7 LANE
  implement / verify / release v0.70.3
  ↓
REAL-LONG-CHAT WINDOW
  run v0.70.3 validation
  + separately capture Exposure target-host B0/E6 preflight
  ↓
REPO EVIDENCE
  classify PASS / HOLD / BLOCK
```

The Exposure preflight may share the practical validation window with v0.70.3, but its evidence and product authority remain separate.

## 9. Final state

```text
LIGHTBOARD_REFERENCE_RESEARCH = MATURE
CONTEXT_PROJECTION_A = PARKED_SHADOW_ONLY
EXPOSURE_B = EVAL_READY / LIVE_DEFERRED
PROVENANCE_C = SUPPORTING / NOT YET DEMANDED
SNAPSHOT_D = BACKLOG
CHECKPOINT_E = BACKLOG
NEXT_RESEARCH_RECOMMENDATION = SOURCE_INTELLIGENCE_FIRST_IMPACT_SCOPE
NEXT_RESEARCH_IMPLEMENTATION = NOT STARTED
3.0M_SCOPE = NOT_FROZEN
PRODUCTION_CHANGE = NONE
```
