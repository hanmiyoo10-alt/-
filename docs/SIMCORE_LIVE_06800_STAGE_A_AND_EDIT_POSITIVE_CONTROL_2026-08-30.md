# SimCore v0.68.0 Live Evidence — Stage A Community Continuity + Genuine Edit Positive Control

Date: 2026-08-30 KST
Status: **LIVE GATE EVIDENCE ACCEPTED**
Classification: **PRODUCT LIVE EVIDENCE · NO RUNTIME CHANGE**

## Production identity

Observed runtime and repository authority agree on:

```text
Version: 0.68.0
Release: Community Parent-Local Alias Classification Repair
release-simcore commit: 6b31a5265f67daf5a90222d6c08bb85f3abde538
release blob: 5094755266444de311ec9cc8ffc7a4dd658e65b1
major checkpoint: M2-5 unchanged
```

The live gate design states that deterministic static/differential fixtures are primary for the nondeterministic target alias. Mandatory live acceptance is an ordinary natural Mode C/COMMUNITY continuity specimen. The target `맘스홀릭 / 예비맘·육아 수다방` and classifier migration receipt are opportunistic when naturally available.

## Accepted Stage A specimen

Runtime:

```text
generation mtehvpha-9atpze
request @2416 -> output @2417
Mode C
```

Observed:

```text
Version: 0.68.0
Probe context: CURRENT TURN
Request hook: SEEN
Core handshake: FOUND
Runtime status: ACTIVE · output COMMITTED
Turn binding: BOUND
Stability: PASS
stale drops: 0
Session load: LOCATION_REUSE
Edit reconcile: SAME_FAST · 0.0 ms
snapshot: UNCHANGED
Prior representation: EXACT
current match: FRESH_CHAT
shape: FRESH_EXACT_CARRYOVER
Output representation: CANONICAL↔FRESH EXACT
Deferred mirror: COMMITTED
Warnings: 0
Compatibility diagnostics: 0
Continuity summary: PASS
Frame sequence: PASS
Frame guard: PASS
Visible chronology: PASS_OR_NOT_APPLICABLE
```

This satisfies the frozen Stage A live contract:

```text
natural Mode C/COMMUNITY
+ v0.68.0 identity
+ BOUND request/output
+ committed output
+ exact/safe mirror commit
+ healthy Structure/Frame/Continuity
+ no new Community classifier fault
= PASS
```

The output used exact-known families (`더쿠`, `X`, `에펨코리아`). It did not naturally exercise the new parent/local descriptor alias branch. That is not required for Stage A because the exact target branch is covered by deterministic integration fixtures and live Stage B is explicitly conditional on natural appearance.

## Genuine visible edit positive control

Between the first and second accepted natural requests, the immediately preceding visible assistant response was manually edited by the user.

Next request:

```text
request @2414 -> output @2415
Mode C
```

Observed edit reconciliation:

```text
Prior representation: EXACT
canonical: 5076:a723484a
fresh:     5076:a723484a
current:   4816:554c2725
current match: NONE
Edit delta: -260 chars vs canonical / Fresh
Edit origin: USER_EDIT_CANDIDATE
Edit reconcile: MANUAL_EDIT_REBUILT · 5.424 s
snapshot: UPDATED
```

Output then returned to the healthy exact path:

```text
Output representation: EXACT
Deferred mirror: COMMITTED
Warnings: 0
THOUGHTS_COMPAT: STRIPPED
Continuity summary: PASS
Frame sequence: PASS
```

Interpretation:

```text
v0.68 Community-only classifier change
→ did not weaken genuine-user-edit attribution
→ did not convert a real edit into Representation carryover
→ preserved USER_EDIT_CANDIDATE -> MANUAL_EDIT_REBUILT
```

This is a direct natural positive control for a frozen unrelated regression required by the v0.68 acceptance matrix.

## Immediate next-turn exact carryover

The following natural request `@2416 -> @2417` then proved that the rebuilt snapshot returned cleanly to ordinary exact carryover:

```text
Prior EXACT
current FRESH_CHAT exact
SAME_FAST 0.0 ms
snapshot UNCHANGED
output EXACT
mirror COMMITTED
warnings 0
```

This closes the edit-control sequence without lingering mutation.

## Performance note

The genuine edit rebuild took:

```text
5.424 s
```

This is not comparable to the separate v0.66 high-severity `40.224 s` rebuild WATCH and therefore does **not** satisfy its promotion condition of another comparable multi-tens-second specimen.

Classification:

```text
5.424 s specimen
= positive-control timing evidence
= NOT a new performance FIX trigger
```

## Separate anomaly excluded from Stage A authority

The earlier natural `@2412 -> @2413` response produced:

```text
THOUGHTS_COMPAT action UNRESOLVED
Knowledge warnings
state quarantine
Deferred mirror OUTPUT_MISMATCH
```

That packet is preserved separately as a WATCH because the output safety gates worked and no evidence ties it to the v0.68 Community classifier mutation. It is not used as the positive Stage A specimen.

See:

`docs/SIMCORE_06800_THOUGHTS_UNRESOLVED_KNOWLEDGE_QUARANTINE_WATCH_2026-08-30.md`

## Stage status

```text
Stage A ordinary Community continuity = PASS
Stage B target parent/local alias live specimen = NOT_EXERCISED / OPTIONAL UNLESS NATURAL
Stage C classifier v2->v3 migration live receipt = NOT_EXERCISED / NON-BLOCKING
Genuine edit frozen regression control = PASS / BONUS DIRECT
Immediate SAME_FAST recovery after edit = PASS / BONUS DIRECT
```

## Verdict

The mandatory v0.68 real-long-chat product gate is satisfied.

```text
06800_REAL_LONG_CHAT_REQUIRED_GATE = PASS
TARGET_ALIAS_NATURAL_LIVE_SAMPLE = NOT_EXERCISED
MIGRATION_NATURAL_LIVE_SAMPLE = NOT_EXERCISED
UNRELATED_ANOMALY = WATCH / SEPARATE
```

No runtime patch is authorized by this evidence record. Terminal `LIVE_PASS` administrative convergence may proceed through the normal release-state path while the separate WATCH remains visible.
