# SimCore Release History — 0.63.x

Date created: 2026-08-27
Primary role: `POINT_IN_TIME_EVIDENCE`
Current-authority status: **NON_AUTHORITATIVE**
Migration status: `ROLLED_OVER`

This file is the release-family historical manifest for SimCore `0.63.x` material that previously accumulated inside `docs/CURRENT_DEVELOPMENT.md`.

It must never be used to infer current production, current live gate, current priority, or current authorization.

## Provenance envelope

```text
Source path: docs/CURRENT_DEVELOPMENT.md
Source commit: ec52c7510f9a12a24c6d1bac6cf655a7b645193b
Source blob: 0d1413ebc0da79a5a7274f17ff4786bc9d850eb5
Exact source snapshot: docs/history/CURRENT_DEVELOPMENT_PRE_ROLLOVER_2026-08-27.md
Migration destination: docs/history/SIMCORE_RELEASE_HISTORY_063.md
Migration class: DOCUMENT_ARCHITECTURE / NON_RUNTIME
```

The exact pre-rollover snapshot reuses the original source blob byte-for-byte. Therefore historical wording, old status claims, timings, hypotheses, failed paths, and measurements remain available without editorial normalization.

This family manifest provides a compact 0.63.x view plus stable original-section identities. If any summary below conflicts with the exact snapshot or a dedicated evidence document, treat the summary as navigation and consult the preserved source evidence.

---

## v0.63.52 — Edit Origin Attribution

Original bounded section identities in the pre-rollover source:

```text
# 9. Completed Major Milestones
## v0.63.52 — Edit Origin Attribution
# 6. Verified Evidence Ledger
## E1 — Genuine user edit positive control
```

Historical contribution:

```text
USER_EDIT_CANDIDATE
REPRESENTATION_DRIFT_CORRELATED
AMBIGUOUS_CHANGE
UNKNOWN
NONE
```

The durable positive control established that a genuine visible user edit can remain distinguishable from exact representation carryover and must continue to use the conservative manual rebuild path when exact representation proof is absent.

Current operative residue is kept in `docs/CURRENT_DEVELOPMENT.md` under **Active Regression Controls**.

---

## v0.63.53 — Boundary-Normalized Envelope Recovery

Original bounded section identities:

```text
# 9. Completed Major Milestones
## v0.63.53 — Boundary-Normalized Envelope Recovery
# 4. Deferred / Waiting for Evidence
## v0.63.53 unresolved suffix validation
# 6. Verified Evidence Ledger
## E2 — Unedited output-side unresolved-envelope failure
```

Historical contract:

- recovery extension was bounded to a Fresh-confirmed compatible unresolved-envelope suffix case;
- raw bodies were not retained;
- non-match remained fail-safe;
- natural special-path activation remained pending rather than being forced.

The original motivating evidence and exact historical wording remain in the pre-rollover snapshot.

---

## v0.63.54 — Safe-Envelope Structural Boundary Reconcile

Original bounded section identities:

```text
# 9. Completed Major Milestones
## v0.63.54 — Safe-Envelope Structural Boundary Reconcile
# 4. Deferred / Waiting for Evidence
## v0.63.54 special-path validation
# 6. Verified Evidence Ledger
## E5 — v0.63.54 ordinary-path regression check
## E7 — trailing-document-newline hypothesis was invalidated before v0.63.54 release
```

Historical contract:

- document-end CR/LF was rejected as the explanation for the fingerprint-level `-1` case;
- only one known internal structural separator variant could be considered after an already-safe envelope candidate existed;
- exact Fresh fingerprint identity remained the confirmation boundary;
- ambiguity remained `OUTPUT_MISMATCH`.

Natural special-path activation remained evidence-driven rather than forced.

---

## v0.63.55 — Representation Fast Reconcile

Original bounded section identities:

```text
# 1. Current Operational State
## Historical validated precursor — v0.63.55
# 2. Historical Validation Release Ledger
## v0.63.55 — Representation Fast Reconcile
# 6. Verified Evidence Ledger
## E-LIVE-055 — Representation Fast Reconcile validated
## E3 — `-1` representation drift → expensive next-turn rebuild
## E4 — `-80` representation drift → expensive next-turn rebuild
## E6 — Deferred Mirror strict safety remains correct
# 9. Completed Major Milestones
## v0.63.55 — Representation Fast Reconcile
```

Historical evidence established the expensive false manual-edit rebuild family:

```text
previous output:
CANONICAL != FRESH_CHAT

next request:
current visible == previous FRESH_CHAT exact
→ REPRESENTATION_DRIFT_CORRELATED
→ old path MANUAL_EDIT_REBUILT
→ observed 4.091 s / 6.257 s examples
```

The release introduced the narrow request-side fast path:

```text
previous OUTPUT_MISMATCH
+ exact prior FRESH_CHAT carryover
+ correct slot/location provenance
→ REPRESENTATION_FAST_RECONCILED
→ snapshot UNCHANGED
```

Real long-chat validation later observed the fast path at 0–2 ms class while preserving the genuine-user-edit rebuild control.

Release identity recorded in the pre-rollover source:

```text
release-simcore commit: 6156685a3edf0ec0c5017900a82990d4f17dfb49
production blob: 8c42851df34831465403d12fc57c7499923bdbc6
```

This behavior remains an active regression control and therefore has a compact current reminder in `docs/CURRENT_DEVELOPMENT.md`.

---

## v0.63.56 — M2-1 Recovery Boundary Split

Original bounded section identity:

```text
# 2. Historical Validation Release Ledger
## M2-1 — Recovery Boundary Split
```

Historical role:

- first physical code-movement checkpoint of the 2.0M Major;
- Recovery responsibilities were split into `output-compat`, `bootstrap-migration`, and a compatibility facade;
- behavior was intentionally mechanical and caller order remained unchanged;
- Representation Fast Reconcile and genuine-edit controls remained frozen;
- persistent schema and protected host surfaces were unchanged.

It is superseded by M2-2 but remains a regression baseline.

---

## v0.63.57 — Current Timeline Authority Guard

Original bounded section identity:

```text
# 2. Historical Validation Release Ledger
## v0.63.57 — Current Timeline Authority Guard
```

Historical trigger:

A natural Mode-A turn exposed a chronology coverage gap where the canonical current timeline was in 2030 but visible output emitted unrequested 2017 timestamps and reverted current character age/state to the historical era.

Historical contract:

```text
known non-broadcast narrative timestamp
→ current_timeline_anchor
→ current timeline authoritative
→ historical context reference-only unless explicitly requested
```

Diagnostics preserved the persisted-state floor and reported visible non-monotonic chronology rather than semantically rewriting generated bodies.

The explicit historical-scene allowance remains part of the current Time/Frame regression freeze.

---

## v0.63.58 — Narrative Tail Time Contract

Original bounded section identity:

```text
# 2. Historical Validation Release Ledger
## v0.63.58 — Narrative Tail Time Contract
```

Historical trigger:

A scene began at a canonical `01:00 AM` frame but visibly reached `03:00 AM` only in prose. The later time was not represented as a canonical timestamp, so the next turn inherited the stale opening time.

Historical contract required canonical visibility for real current/end-time advancement while preserving line-level monotonic Time commit semantics and avoiding arbitrary prose-time auto-commit.

Current operative residue is the frozen narrative terminal-time observability / monotonic-floor rule in `docs/CURRENT_DEVELOPMENT.md`.

---

## v0.63.59 — Broadcast End Closure Contract

Original bounded section identity:

```text
# 2. Historical Validation Release Ledger
## v0.63.59 — Broadcast End Closure Contract
```

Historical role:

- direct production parent of v0.64.0 M2-2;
- closed the B_END terminal-airtime/structure contract before Representation ownership movement resumed;
- required explicit canonical terminal timestamp handling for B_END;
- preserved the fixed COMMUNITY closure structure.

Recorded historical identity:

```text
release commit: 7c0f6f4a8e0b7e42a5996dc7bacd149f27e3751d
release blob: da47e5d8123c0abfe9902f016c84ac758f766032
```

The Broadcast/Time/Frame behavior remains part of the current hard freeze.

---

# Historical evidence bundle from the old CURRENT document

The following old evidence-ledger units are preserved exactly in the pre-rollover snapshot:

```text
E-LIVE-055 — Representation Fast Reconcile validated
E1 — Genuine user edit positive control
E2 — Unedited output-side unresolved-envelope failure
E3 — `-1` representation drift → expensive next-turn rebuild
E4 — `-80` representation drift → expensive next-turn rebuild
E5 — v0.63.54 ordinary-path regression check
E6 — Deferred Mirror strict safety remains correct
E7 — trailing-document-newline hypothesis was invalidated before v0.63.54 release
```

Those units are point-in-time evidence. They do not become current merely because this family manifest references them.

---

# Current-authority boundary

The following must be read from `docs/CURRENT_DEVELOPMENT.md`, not from this file:

```text
current production version
current release commit/blob
current live gate
current priority
current stop/advance condition
current hard freeze
```

This historical family ends as evidence; it never authorizes a runtime change.
