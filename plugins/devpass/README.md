# DevPass fixed update-channel compatibility path

The canonical DevPass project/declaration now lives at:

`plugins/risu/local/devpass/README.md`

This legacy root remains active for the fixed update artifact target only:

`plugins/devpass/latest.js`

Fixed update URL:

```text
https://raw.githubusercontent.com/hanmiyoo10-alt/-/main/plugins/devpass/latest.js
```

Current repository state: `latest.js` is not present. Its artifact state remains `DECLARED_MISSING` and the production version remains `UNKNOWN` until owning evidence establishes otherwise.

Do not create a placeholder artifact or infer a release merely to satisfy this compatibility path.
