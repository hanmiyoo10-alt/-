# 포춘골프 실행 영상 확인 (2026-09-08)

## 자료와 한계

사용자가 작업 재개를 요청하여 보존된 최신 영상 `1000014768.mp4`(15.675544초)를 확인했다. 영상 업로드 시각은 2026-09-08 04:59:23 UTC다. 영상 자체에는 버전 표시가 없으나, 사용자가 후속 질문에 'ㅇㅇ'으로 답하여 **Fortune Golf Probe 5에서 촬영한 영상임을 확인했다.** 빌드 식별 근거는 사용자 확인이다.

## 직접 관찰

2초 간격 표본 프레임(0–14초 구간)을 확인했다.

- 초반: `Invalid memory access; address: 4`, `net/wie/EventQueue.getNextEvent([I)V` 오류 창.
- 중간: 흰 화면, 이후 검은 게임 영역. 상단에 자홍색 배경의 작은 상태 아이콘이 보인다.
- 약 12초: `Invalid memory access; address: 0`, 같은 EventQueue 메서드 오류 창. 표시된 PC와 LR은 0이다.
- 끝부분: WIE 라이브러리 화면으로 돌아온다.
- 표본에서 타이틀 메뉴와 실제 골프 플레이는 확인되지 않는다.

## 판단

이 영상으로 BMP RGB565 패치의 줄무늬 개선이나 악화를 판정할 수 없다. 오류 창의 EventQueue 이름만으로 큐 구현이 원인이라고 확정할 수 없다. 콜백·초기화 경로 조사 후보는 유지하되, PC=0만으로 특정 콜백 결함을 단정하지 않는다. 처음 오류가 나기 전 동작은 영상에 없으므로 무입력 첫 실행 재현으로 확정하지 않는다.

## 다음 확인

Probe 5에서도 시작 오류가 발생한 것으로 기록한다. Probe 4에서도 주소 4 오류가 있었으므로 RGB565 변경이 새 오류를 만들었다고 단정하지 않는다. 초기화 및 타이머 콜백 경로를 조사한다. 이번 작업에서는 호환 코드 변경이나 새 APK 빌드를 하지 않았다.
