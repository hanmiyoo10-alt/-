# Failures — SERVER-LARGE-DOC-BACKEND-ADAPTER

아직 구현/CI/deploy를 시작하지 않았다.

확인된 실패 없음.

향후 기록 대상:
- path/workspace boundary failure
- session/chunk consistency failure
- source-changed conflict handling failure
- PocketRisu adapter timeout/error isolation failure
- CI/integration permission failure

예상과 다른 결과가 나오면 추측으로 덮어쓰지 않고 여기 근거와 함께 기록한 뒤 원인을 확인한다.
