# AI Programming Study

Status: `ACTIVE`
Started: `2026-09-14`

이 문서는 `AI 프로그래밍입문` 공부를 PSAT 등 다른 공부 주제와 분리해 추적하는 과목별 진입점입니다.

## Source locator

현재 확인된 수업 자료 위치:

```text
Google Drive
→ 학교_전공공부
→ AI 프로그래밍입문
→ 1주차
→ 2주차
```

### 1주차에서 확인된 자료명

- `01파이썬 소개(입문).pdf`
- `01파이썬 소개(입문)_260829_143434.pdf`

### 2주차에서 현재 확인된 자료명

- `02변수와연산자.pdf`
- `02_변수와 자료형 1.mp4`
- `02_변수와 자료형 2.mp4`
- `음성 260914_130640.m4a`
- `Untitled1.ipynb의 사본`
- `Untitled0.ipynb의 사본`

별도 녹음 파일은 이제 실제 `audio/mp4` 자료로 확인되었습니다. 따라서 2주차 참고자료는 PDF, 영상, 녹음, Colab 실습 사본을 함께 사용할 수 있습니다.

현재 확인된 실제 Colab 실습 위치:

```text
Google Drive
→ 코랩
→ Untitled1.ipynb
```

또한 `AI 프로그래밍입문/2주차`에는 `Untitled1.ipynb의 사본`이 별도 실습 사본으로 존재합니다. 사본은 같은 표시 이름으로 다시 만들어질 수 있으므로 Drive ID를 public repository에 저장하지 않고, 매번 최신 폴더 상태를 다시 확인합니다.

`Untitled1.ipynb`는 2026-09-14 16:57 KST에 마지막 수정된 노트북으로 확인되었고, 사용자가 지정한 `오후 4:57` 자료와 일치합니다.

드라이브 파일 ID, 공유 링크, 개인식별정보, 교수 연락처 등은 public repository에 복제하지 않습니다.

## Current learning scope

현재 자료에서 확인된 초반 학습 범위는 다음과 같습니다.

1. Python과 Colab 기본 사용법
2. 코드 셀 실행과 `print()`
3. 기본 자료형 `int`, `float`, `str`, `bool`
4. 변수와 동적 타이핑
5. `input()`과 형 변환
6. 산술·할당 연산자
7. 간단한 계산 문제를 코드로 표현하기
8. 문자열 포맷팅과 f-string
9. 문자열 내부 따옴표와 이스케이프 문자

현재 Colab 노트북에는 위 범위를 직접 시행착오로 풀어 본 코드와 메모가 누적되어 있습니다. 이 노트북을 현재 실습 증거의 우선 진입점으로 사용합니다.

## Study method

초기 기본 루프는 다음처럼 둡니다.

```text
개념 1개 확인
→ 짧은 코드 직접 작성
→ 실행 전 결과 예측
→ Colab에서 실행
→ 결과가 다르면 원인을 가장 좁은 범주로 분류
→ 비슷한 예제로 다시 확인
```

오류/혼동은 가능하면 다음처럼 구분합니다.

```text
SYNTAX
TYPE
INPUT_CONVERSION
OPERATOR
VARIABLE_STATE
OUTPUT_FORMAT
CONCEPT
```

이 분류가 실제 학습에 도움이 되는지는 문제/실습 증거가 쌓인 뒤 판단합니다.

## Current source state

`오후 4:57` Colab 자료는 `코랩/Untitled1.ipynb`로 식별 완료되었습니다.

`AI 프로그래밍입문/2주차` 폴더에는 현재 PDF 1개, MP4 영상 2개, M4A 녹음 1개, Colab 노트북 사본들이 확인됩니다. 이전의 `2주차 폴더가 비어 있음` 및 `별도 녹음 파일 미확인` 상태는 더 이상 현재 상태가 아닙니다.

## Immediate next action

최신 `Untitled1.ipynb의 사본`의 마지막 학습 상태를 기준으로 f-string 출력 형식과 문자열/이스케이프 구간을 이어서 학습하고, 필요하면 2주차 PDF·영상·녹음을 같은 개념의 참고자료로 대조합니다.
