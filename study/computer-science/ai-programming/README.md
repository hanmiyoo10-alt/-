# AI Programming Study

Status: `ACTIVE`
Started: `2026-09-14`

이 문서는 `AI 프로그래밍입문` 공부를 PSAT 등 다른 공부 주제와 분리해 추적하는 과목별 진입점입니다.

## Source locator

현재 확인된 수업 자료 위치:

```text
Google Drive
→ 학교_전공공부
→ AI 프로그래밍입문
→ 1주차
```

확인된 자료명:

- `01파이썬 소개(입문).pdf`
- `01파이썬 소개(입문)_260829_143434.pdf`
- `02변수와연산자.pdf`

드라이브 파일 ID, 공유 링크, 개인식별정보, 교수 연락처 등은 public repository에 복제하지 않습니다.

## Current learning scope

현재 자료에서 확인된 초반 학습 범위는 다음과 같습니다.

1. Python과 Colab 기본 사용법
2. 코드 셀 실행과 `print()`
3. 기본 자료형 `int`, `float`, `str`, `bool`
4. 변수와 동적 타이핑
5. `input()`과 형 변환
6. 산술·할당 연산자
7. 간단한 계산 문제를 코드로 표현하기

## Study method

초기 기본 루프는 다음처럼 둡니다.

```text
개념 1개 확인
→ 짧은 코드 직접 작성
→ 실행 전 결과 예측
→ Colab에서 실행
→ 결과가 다르면 원인을 가장 좁은 범주로 분류
→ 비슷한 예제로 다시 확인
```

오류/혼동은 가능하면 다음처럼 구분합니다.

```text
SYNTAX
TYPE
INPUT_CONVERSION
OPERATOR
VARIABLE_STATE
OUTPUT_FORMAT
CONCEPT
```

이 분류가 실제 학습에 도움이 되는지는 문제/실습 증거가 쌓인 뒤 판단합니다.

## Current uncertainty

사용자가 언급한 `오후 4:57`의 Colab 자료는 현재 Drive 메타데이터만으로 정확한 파일을 특정하지 못했습니다.

따라서 현재는 수업 PDF를 authoritative study source로 사용하고, 해당 Colab 노트북이 식별되면 이 문서의 source locator에 파일명 수준으로만 추가합니다.

## Immediate next action

`01 파이썬 소개`와 `02 변수와 연산자` 범위에서 짧은 코드 문제를 직접 풀면서, 실행 전 예측과 실제 결과의 차이를 기록합니다.
