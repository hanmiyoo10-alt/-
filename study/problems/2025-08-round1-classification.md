# 2025년 8월 1회차 난이도 분류

학습 목표: 이론형/실기연계형을 가리지 않고 `하·중` 난이도까지 전부 맞히기.

판정 기준:
- 하: 한 개념을 알고 있으면 바로 답하거나 한 단계 적용으로 끝나는 문제
- 중: 여러 개념 비교, 2단계 이상 적용, 실제 계산/추적이 필요한 문제
- 상: 여러 개념을 결합한 복합 응용형

교재 자체 A/B/C/D 표시는 출제 빈도 등급이며 아래 학습 난이도와 별개다.

## 1과목 소프트웨어 설계 1~20
- 하: 1, 2, 3, 4, 6, 7, 8, 9, 11, 12, 14, 17, 18, 19, 20
- 중: 5, 10, 13, 15, 16
- 상: 없음

## 2과목 소프트웨어 개발 21~40
- 하: 22, 25, 26, 27, 28, 29, 33, 34, 35, 36, 38, 39, 40
- 중: 21, 23, 24, 30, 31, 32, 37
- 상: 없음

> 정정: 36번은 클린 코드 문제가 아니라 `동치 분할 검사(Equivalence Partitioning Testing)`를 설명에서 판별하는 문제다. 난이도 하 판정은 유지한다.

## 3과목 데이터베이스 구축 41~60

### 하 난이도
- 41: DAS 연결 프로토콜 기본 구분. iSCSI는 네트워크 기반이므로 DAS와 구별.
- 44: 정규화의 핵심이 릴레이션 합병이 아니라 분해라는 기본 개념.
- 45: 범위/해시/조합/목록/라운드로빈 등 파티션 유형의 기본 회상.
- 48: 자료 사전은 DBMS가 생성·유지하며 일반 사용자가 직접 갱신하지 않는다는 기본 개념.
- 49: DML과 DDL 구분. ALTER는 DDL.
- 52: 분산 데이터베이스의 분산 투명성 기본 개념.
- 53: ACID 중 일관성(Consistency) 정의.
- 55: 1:N 관계의 기본 판별.
- 56: 데이터 모델 구성 요소 중 연산(Operation)의 정의.
- 57: 도메인(Domain)의 정의.
- 58: `GRANT UPDATE ON STUDENT TO PARK` 기본 권한 부여 구문.

### 중 난이도
- 42: `ON DELETE NO ACTION / CASCADE / SET NULL / SET DEFAULT`를 서로 구별해야 하는 참조 무결성 옵션 문제.
- 43: 관계대수 수학 표현식을 읽고 카티션 프로덕트를 판별.
- 46: 뷰의 여러 특성을 동시에 판별.
- 47: 릴레이션의 여러 특성을 동시에 판별.
- 50: 관계대수 집합 연산별 카디널리티 성질을 적용해 교집합 판별.
- 51: 외래키의 NULL, 중복, 기본키 일부 가능 여부, 참조 무결성 등 여러 성질을 동시에 판별.
- 54: 중첩 SELECT/서브쿼리를 순서대로 실행해 결과를 구하는 SQL 실행형.
- 59: `DROP VIEW ... RESTRICT`와 뷰 의존 관계를 함께 적용.
- 60: 인덱스의 검색 성능, B-Tree, 범위 검색, 생성·변경 가능 여부 등 여러 특징을 동시에 판별.

### 집계
- 하: 11문제
- 중: 9문제
- 상: 없음

## 4과목 프로그래밍 언어 활용 61~80

### 하 난이도
- 62: PCB를 가지며 CPU를 할당받아 실행되는 프로그램이라는 설명에서 프로세스를 판별.
- 64: C의 관계/논리 연산자 `<=`, `||`, `&&`, `!`를 이용한 짧은 출력 추적.
- 65: OSI 7계층에서 연결 생성·관리·종료와 토큰을 단서로 세션 계층을 판별.
- 67: 드모르간 법칙으로 `!a && !b`와 같은 의미의 조건식을 판별.
- 68: C 산술 연산자와 대입 연산자 `=`의 기본 구분.
- 69: HRN의 대기시간·서비스시간, SJF 보완, 우선순위 계산값의 기본 특징 판별.
- 70: 입력 문자 `c`에 따라 `switch`의 해당 case로 이동해 출력하는 단순 분기 추적.
- 71: Best Fit으로 17KB를 20KB 공간에 배치하고 내부 단편화 3KB를 구하는 1단계 적용.
- 72: OSI 7계층에서 동기화·오류제어·흐름제어를 단서로 데이터 링크 계층 판별.
- 73: 파일 권한 644에서 `666 - umask = 644`를 적용해 umask 022를 구하는 기본 계산.
- 74: C `malloc()`의 동적 메모리 할당 단위(Byte), `free`, NULL 반환 등의 기본 특징 판별.
- 79: Python 조건문에서 실행 결과가 20이 되도록 `elif` 계열 문법을 채우는 기본 문법 문제.

### 중 난이도
- 61: C의 콤마 연산자와 전위/후위 증감 연산이 연속으로 적용되는 상태 추적.
- 63: IPv4/IPv6의 헤더 길이, 자동 설정, 주소 체계 등 여러 특징을 동시에 비교.
- 66: `/24` 네트워크를 FLSM 4개로 분할한 뒤 4번째 네트워크의 사용 가능한 IP를 계산.
- 75: Java 배열을 반복문으로 순회하면서 조건에 따라 요소 값을 변경하는 실행 결과 추적.
- 76: Java 메소드 호출과 `try`/예외 처리/`finally`의 실행 흐름을 따라 출력 결과를 판별.
- 77: 프로세스 상태 전이, PCB, 문맥 교환, 스레드와 프로세스 관계를 동시에 비교.
- 78: 순차 파일의 일괄 처리 적합성, 저장 효율, 매체 변환, 삽입·삭제·수정 특성을 함께 판별.
- 80: 3개 페이지 프레임에서 FIFO 페이지 교체를 순서대로 수행해 최종 프레임 상태를 구함.

### 집계
- 하: 12문제
- 중: 8문제
- 상: 없음

### 1~80 누적
- 하: 51문제
- 중: 29문제
- 상: 0문제

하 난이도는 즉답 수준을 목표로 하고, 중 난이도는 풀이 과정과 판별 기준까지 설명 없이 재현할 수 있는 상태를 목표로 한다.
