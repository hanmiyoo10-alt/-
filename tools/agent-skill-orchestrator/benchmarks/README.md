# O4 Retrospective Role Benchmarks

This directory contains deterministic, retrospective-only benchmark primitives. O4-A defines schemas, exact fixture-owned matching, role metric vectors, and micro aggregation only. It performs no model execution, selects no model-family winner, and creates no role assignment authority.
